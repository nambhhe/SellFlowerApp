<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sellFlower.app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
<!--        android:theme="@style/AppTheme">-->

        <!-- LoginActivity as the launcher activity -->
        <activity android:name=".controller.activity.LoginActivity"
            android:theme="@style/Theme.AppCompat"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity without launcher intent-filter -->
<!--        <activity android:name=".controller.activity.MainActivity" />-->

        <!-- Other activities -->
<!--        <activity android:name=".controller.activity.SignUpActivity" />-->
        <activity android:name=".controller.activity.FlowerDetailActivity" />

        <!-- ... other components ... -->

    </application>

</manifest>